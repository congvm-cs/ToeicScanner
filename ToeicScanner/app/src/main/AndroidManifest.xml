<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nguyenantin.toeicscanner">

    <uses-permission android:name="android.permission.CAMERA"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.front.autofocus"
        android:required="true" />

    <!-- App config -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logotoeicscanner"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logotoeicscanner"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Loading"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SubmitIdTest"
            android:screenOrientation="landscape" />
        <activity
            android:name=".CustomCamera"
            android:screenOrientation="landscape" />
        <activity
            android:name=".CheckPicture"
            android:screenOrientation="landscape" />
        <activity
            android:name=".CheckTest"
            android:screenOrientation="landscape" />
    </application>

</manifest>